import React from 'react';
import { Archive } from 'lucide-react';

// This is a placeholder component to fix the import error in WorkflowManagement.tsx
// The Archive icon is imported from lucide-react but not defined as a component

export default Archive;