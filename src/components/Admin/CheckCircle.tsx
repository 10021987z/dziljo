import React from 'react';
import { CheckCircle } from 'lucide-react';

// This is a placeholder component to fix the import error in InvoiceManagement.tsx
// The CheckCircle icon is imported from lucide-react but not defined as a component

export default CheckCircle;